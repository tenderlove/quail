if BUILD_EXCHANGE
DIR_EXCHANGE = exchange
endif

if BUILD_CAMERA
DIR_CAMERA = camera
endif

if BUILD_CHAT
DIR_CHAT = chat
endif

SUBDIRS = $(DIR_EXCHANGE) $(DIR_CAMERA) $(DIR_CHAT)
DIST_SUBDIRS = exchange camera chat
